node_modules/
.next/
.env*.local
*.log
.DS_Store
.vercel
.turbo

# TypeScript incremental build cache — regenerated by `tsc`/`next build`.
# Committing it adds churn to every PR and never helps a reviewer.
*.tsbuildinfo

# Playwright runtime artifacts.
test-results/
playwright-report/
playwright/.cache/
