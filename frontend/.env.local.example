# Where the Python API lives. Server-side fetches happen from the Next.js
# runtime, so this needs to be reachable from wherever Next.js is deployed.
#
# Local dev (FastAPI on default port):
API_URL=http://localhost:8000

# Vercel + remote VPS deployment example:
# API_URL=https://api.cyberalertx.com
