"""Operational CLI tools.

Each module is a self-contained `python -m cyberalertx.tools.<name>`
entry point. Reserved for ops tasks that shouldn't pollute the main CLI
(migrations, backfills, validators).
"""
